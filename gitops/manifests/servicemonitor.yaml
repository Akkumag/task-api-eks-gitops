apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: task-api
  namespace: task-api
spec:
  selector:
    matchLabels:
      app: task-api
  endpoints:
    - port: http
      path: /metrics